import Vue from 'vue';
import actions from './actions';
import getters from './getters';

const initState = {
};

const mutations = {
};

export default {
    state: initState,
    mutations,
    actions,
    getters,
};
